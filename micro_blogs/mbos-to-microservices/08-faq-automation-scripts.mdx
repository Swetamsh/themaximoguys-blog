---
title: "Do My Automation Scripts Work in MAS 9?"
type: "faq"
platform: ["linkedin", "x", "instagram"]
sourceBlog:
  title: "From MBOs to Microservices: The Application Developer's Cloud-Native Journey"
  slug: "from-mbos-to-microservices"
  series: "Developer Mindset Shift"
  part: 2
  url: "posts/THINK-MAS/2026-02-05-dev-mindset-02-mbos-to-microservices.mdx"
date: "2026-02-18"
author: "The Maximo Guys"
tags: ["MAS 9", "Development", "Automation Scripts"]
status: "draft"
carouselReady: false
infographicReady: false
---

**FAQ: Do my automation scripts work in MAS 9?**

**Short answer:** Yes.

**Longer answer:** Yes — migrate them, test them, and watch for three specific patterns.

Here is what you need to know:

**What still works:**

The scripting engine is still there. Python (Jython) and JavaScript are still supported. Event triggers — save, init, action, validate, before and after — still fire. The implicit variables (`mbo`, `mboset`, `service`) still exist. If your script uses these in standard ways, migration is straightforward.

**Watch for pattern 1: Direct SQL access**

Scripts that use `SqlFormat`, `DBManager`, or raw database calls are at risk. MAS 9 prefers MboSet-based data access. Audit your scripts for direct SQL and refactor before migration.

**Watch for pattern 2: External service network calls**

Scripts that call external services need a network connectivity review. Container networking works differently than on-premise networking. A script calling an internal IP that worked in 7.6 may need a DNS name and a network policy update in MAS.

**Watch for pattern 3: Deprecated API references**

Some internal Maximo Java classes have changed or moved between 7.6 and MAS Manage's codebase. Scripts that reference specific Java classes by full package path need a compatibility check.

**The bottom line:**

Don't panic. Don't assume your scripts are broken. Run them in a non-production MAS environment, catch the failures, fix the patterns above. Most scripts migrate cleanly.

Full breakdown in the blog linked below.

---

*Part 2 of the Developer Mindset Shift series.*
